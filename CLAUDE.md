# CLAUDE.md - 全セッション共通ルール

## 基本方針

- 全ての開発・ファイル更新はこのGitHubリポジトリ経由で行う
- コード変更後は必ずコミット＆プッシュする（ユーザーに確認不要）
- MacBook Proはauto-syncで自動同期されるため、GitHub更新 = Mac更新
- 二度手間を避ける。同じ作業を2回させない

## ワークフロー

```
ユーザー（スマホ）→ Claude Code → GitHub → MacBook Pro（自動同期）
```

1. ユーザーがスマホからClaude Codeに指示
2. Claude Codeがコードを書く・編集する
3. 即座にコミット＆プッシュ
4. MacBook Proが自動で最新を取得（60秒以内）

## コミットルール

- 変更したら即コミット＆プッシュ。溜めない
- コミットメッセージは日本語OK
- ブランチはmainを基本とする（指定がない限り）

## ファイル管理

- プロジェクトファイルは全てこのリポジトリで管理
- Mac側のファイルを直接編集する指示が来た場合、このリポジトリに反映してauto-syncに任せる
- 不要なファイルを作らない。必要最小限

## コミュニケーション

- 日本語で対応
- 簡潔に。余計な説明不要
- やったらすぐ報告。「完了。プッシュ済み」で十分
